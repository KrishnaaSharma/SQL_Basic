-- create database ---
create database collage;

-- use active database ---
use collage;

-- create table ----
create table users(
ar_id int,
f_name varchar(30),
l_name varchar(30),
heroic_name varchar(30),
city varchar(30)
);
